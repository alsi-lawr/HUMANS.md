# Casefile Codex adapter

Casefile owns Codex model-catalog overrides, V1 feature flags, profiles, and role bindings. Run `casefile-codex-setup` only after core migration or core setup has completed. Its receipt is isolated under `~/.codex/backups/casefile/`; uninstall removes only `casefile@humans-md`, never the shared marketplace or other plugins.
