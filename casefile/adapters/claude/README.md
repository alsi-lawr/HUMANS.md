# Casefile Claude adapter

The Casefile package supplies the governed workflow skills, role agents, matrices, and profiles for
Claude. It has no standing-contract lifecycle: install `humans-md` first when that contract is
wanted. Removing Casefile never removes the marketplace or other plugins.
